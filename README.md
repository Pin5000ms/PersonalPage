# Personal Page

Vue 3 + Vite + TypeScript personal website scaffold for GitHub Pages.

## Local development

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

## Deploy

Push to `main` and enable GitHub Pages with `GitHub Actions` as the source.
